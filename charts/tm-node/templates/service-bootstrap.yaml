{{if (default .Values.bootstrap.enabled true)}}
{{- range untilStep 1 5 1 }}
---
apiVersion: v1
kind: Service
metadata:
  name: syn-node-{{ . }}
  namespace: {{ $.Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 26656
      name: synd-p2p
    - port: 26657
      name: synd-rpc
  selector:
    syn.network/node: "{{ . }}"
{{- end }}
{{ end }}
